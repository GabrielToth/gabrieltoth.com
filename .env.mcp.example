# MCP tokens — copy lines you need into .env.local (never commit .env.local)
# GitHub: ❌ DO NOT COMMIT | Vercel: N/A (MCP runs in Cursor IDE only)
#
# Cypress Cloud MCP: https://cloud.cypress.io → Profile → MCP personal access token
CYPRESS_MCP_TOKEN=your-cypress-mcp-token

# Optional: other MCP providers (if you add servers in .cursor/mcp.json)
# CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
