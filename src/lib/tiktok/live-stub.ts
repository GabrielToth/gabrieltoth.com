/**
 * TikTok Live API — Not Available
 *
 * TikTok does not provide an official public API for live streaming data
 * (chat, gifts, viewers, stream status, etc.). The following third-party
 * services offer unofficial access but come with Terms of Service risks:
 *
 * - TikTool (tik.tools) — WebSocket relay + REST, paid from $7/week
 * - TikTokLive (Python, open-source) — reverse-engineered, not production-safe
 *
 * TikTok Shop Live Shopping is managed through a separate Partner API
 * (partner.tiktokshop.com) and covers only commerce, not stream data.
 *
 * When TikTok releases an official Live API, this file should be replaced
 * with a real implementation matching the pattern in src/lib/facebook/live.ts.
 */
